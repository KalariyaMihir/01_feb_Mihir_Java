#include<stdio.h>
void main()
{
    int a,b;
    printf("Enter the value of two number : ");
    scanf("%d%d",&a,&b);
    (a==b) ? (printf("Both are Equal")) : (printf("both are not Equal"));
    
}