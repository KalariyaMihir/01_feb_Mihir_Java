// WAP to find Max Number From Given 
 
 #include<stdio.h>
 int main()
 {
    int a,b;
    printf("Enter First Value :");
    scanf("%d",&a);
    printf("Enter Second Value :");
    scanf("%d",&b);
    (a>b) ? (printf("First Value is MAx")) : (printf("Second Value is Max"));
 }